CREATE TABLE PageInfoEntity (
    name TEXT PRIMARY KEY,
    page INTEGER NOT NULL
);

selectPageInfo:
    SELECT *
    FROM PageInfoEntity
    WHERE name = :name;

delete:
    DELETE FROM
    PageInfoEntity;

updatePage:
UPDATE PageInfoEntity SET page = ? WHERE name = ?;

insertPage:
INSERT OR IGNORE INTO PageInfoEntity(name, page) VALUES ?;
